#ifndef Queue_h
#define Queue_h

// The Node data structure
struct Node {
    long data;
    Node* next;
    
    Node(long d) {
        data = d;
        next = NULL;
    }
    
    ~Node(){
        
    }
};


struct Queue {
	N = new Node(value)
		last.next = N;
	last = N;
};

#endif
